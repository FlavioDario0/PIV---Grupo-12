package com.kineo.backend.config;

// Classe temporariamente limpa para permitir o fluxo do login manual
public class SecurityConfig {

    // O seu AuthService já está a validar o e-mail e a senha manualmente.
    // Quando o projeto for evoluir para usar Tokens JWT no futuro,
    // a configuração do Spring Security voltará para aqui!

}